# 项目协作约定

## 完成与发布

- 当任务要求修改或构建本仓库内容时，在改动完成且相关检查通过后，主动提交并推送，不需要等待用户再次说“推送”。
- 延续本项目现有发布方式：若当前位于 `main`、本地与远端同步且用户没有要求使用分支或 Pull Request，则直接提交并推送到 `origin/main`。
- 只暂存和提交本次任务相关文件；不得提交 `outputs/`、临时文件或用户的其他无关改动。
- 提交前运行与改动风险相匹配的检查，并执行 `git diff --check`；检查失败时先修复，不得把失败结果当作完成。
- 涉及 GitHub Pages 页面时，推送后等待部署工作流结束，并核对线上页面的关键结果后再向用户报告完成。
- 不得把 API Key、访问令牌或其他密钥写入仓库、提交记录或日志；只允许通过环境变量或 GitHub Secrets 使用。
- 若提交、推送或部署失败，应明确报告失败环节和原因，不得声称已经发布成功。

## 非修改任务

- 只读检查、解释、评审或建议类任务不创建提交，也不推送。
